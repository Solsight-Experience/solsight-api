import { JupiterTokenMintInformation } from "src/infra/jupiter/types";
import { Token } from "../entities/token.entity";

export function mapToToken(data: JupiterTokenMintInformation): Partial<Token> {

}
